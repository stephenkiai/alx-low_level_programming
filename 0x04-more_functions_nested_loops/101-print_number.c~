#include "main.h"
/**
 * print_number - prints an integer
 * @n is the integer
 * Description - it prints an integer
 * return: 0 always
 *
 */
void print_number(int n)
{
print_number("%d", n);
print_number('\n');
return (0);
}
